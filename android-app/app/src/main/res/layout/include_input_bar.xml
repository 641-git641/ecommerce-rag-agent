<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_input_frosted"
    android:paddingHorizontal="14dp"
    android:paddingTop="12dp"
    android:paddingBottom="10dp">

    <EditText
        android:id="@+id/et_chat_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:hint="@string/hint_input"
        android:inputType="textMultiLine|textCapSentences"
        android:maxLines="5"
        android:minHeight="38dp"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/input_hint"
        android:textSize="13sp"
        android:paddingHorizontal="6dp"
        android:paddingVertical="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 图片缩略图预览区域 -->
    <LinearLayout
        android:id="@+id/ll_image_preview"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone"
        android:layout_marginTop="6dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/et_chat_input">

        <ImageView
            android:id="@+id/iv_image_preview"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:scaleType="centerCrop"
            android:background="@drawable/bg_input_frosted" />

        <ImageView
            android:id="@+id/btn_remove_image"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_close_small"
            android:padding="4dp"
            android:tint="@color/input_icon_tint"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:layout_marginStart="6dp"
            android:contentDescription="移除图片" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_selected_cards_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:clipToPadding="false"
        android:visibility="gone"
        android:layout_marginTop="6dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/ll_image_preview" />

    <ImageView
        android:id="@+id/btn_image_input"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_image"
        android:padding="4dp"
        android:alpha="0.7"
        android:tint="@color/input_icon_tint"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="图片"
        app:layout_constraintEnd_toStartOf="@+id/btn_voice"
        app:layout_constraintBottom_toBottomOf="@id/rv_selected_cards_input"
        app:layout_constraintTop_toBottomOf="@id/ll_image_preview"
        android:layout_marginTop="6dp" />

    <ImageView
        android:id="@+id/btn_voice"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_mic"
        android:padding="4dp"
        android:alpha="0.7"
        android:tint="@color/input_icon_tint"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="语音输入"
        app:layout_constraintEnd_toStartOf="@+id/btn_send_msg"
        app:layout_constraintBottom_toBottomOf="@id/rv_selected_cards_input"
        app:layout_constraintTop_toBottomOf="@id/ll_image_preview"
        android:layout_marginTop="6dp" />

    <ImageView
        android:id="@+id/btn_send_msg"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_send"
        android:padding="4dp"
        android:tint="@color/send_tint"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="发送"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/rv_selected_cards_input"
        app:layout_constraintTop_toBottomOf="@id/ll_image_preview"
        android:layout_marginTop="6dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
